apiVersion: v1
kind: Service
metadata:
  name: todo-app-service
  namespace: fastapi-app
spec:
  selector:
    app: todo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer  # Use ClusterIP for internal or NodePort if no LoadBalancer available
